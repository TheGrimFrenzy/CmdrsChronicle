{
  "category": "Trade",
  "title": "Black Market Activity",
  "chartType": "summary-tile",
  "query": "SELECT COUNT(*) AS count, COALESCE(SUM(TotalSale), 0) AS totalReward FROM MarketSell WHERE event_timestamp >= :startDate AND event_timestamp < :endDate AND (BlackMarket = 1 OR IllegalGoods = 1)",
  "details": {
    "text": "{count} illicit sale(s) · {totalReward} Cr proceeds",
    "help": "Sales flagged as BlackMarket=1 or IllegalGoods=1"
  }
}
