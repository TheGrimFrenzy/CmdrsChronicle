{
  "category": "PassengerTransport",
  "title": "Passenger Missions",
  "chartType": "summary-tile",
  "query": "SELECT COUNT(*) AS count, COALESCE(SUM(Reward), 0) AS totalReward FROM MissionCompleted WHERE event_timestamp >= :startDate AND event_timestamp < :endDate AND Name LIKE 'Mission_Passenger%'",
  "details": {
    "text": "{count} passenger mission(s) completed · {totalReward} Cr earned",
    "help": "Missions with names matching 'Mission_Passenger%'"
  }
}
