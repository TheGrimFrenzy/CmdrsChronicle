{
  "category": "Mining",
  "title": "Cargo Ejected",
  "chartType": "summary-tile",
  "query": "SELECT COUNT(*) AS count, SUM(CASE WHEN Abandoned = 1 THEN 1 ELSE 0 END) AS abandoned FROM EjectCargo WHERE event_timestamp >= :startDate AND event_timestamp < :endDate",
  "details": {
    "text": "{count} cargo unit(s) ejected · {abandoned} abandoned",
    "help": "EjectCargo events — Abandoned=1 means left for others to collect"
  }
}
