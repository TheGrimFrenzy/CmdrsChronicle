{
  "category": "Missions",
  "title": "Wing Missions",
  "chartType": "summary-tile",
  "query": "SELECT COUNT(*) AS count FROM MissionAccepted WHERE event_timestamp >= :startDate AND event_timestamp < :endDate AND Wing = 1",
  "details": {
    "text": "{count} wing mission(s) accepted",
    "help": "Missions with the Wing flag set, requiring multiple commanders"
  }
}
