{
  "category": "MaterialGathering",
  "title": "Materials by Category",
  "chartType": "bar-chart",
  "query": "SELECT COUNT(*) FROM MaterialCollected WHERE event_timestamp >= :startDate AND event_timestamp < :endDate",
  "detailQuery": "SELECT Category AS label, CAST(SUM(Count) AS INTEGER) AS value FROM MaterialCollected WHERE event_timestamp >= :startDate AND event_timestamp < :endDate GROUP BY Category ORDER BY value DESC"
}
