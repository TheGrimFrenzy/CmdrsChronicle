{
  "category": "SettlementActivities",
  "title": "Items Collected On-Foot",
  "chartType": "bar-chart",
  "query": "SELECT COUNT(*) FROM CollectItems WHERE event_timestamp >= :startDate AND event_timestamp < :endDate",
  "detailQuery": "SELECT Type AS label, CAST(SUM(Count) AS INTEGER) AS value FROM CollectItems WHERE event_timestamp >= :startDate AND event_timestamp < :endDate GROUP BY Type ORDER BY value DESC"
}
