{
  "category": "SalvageAndRecovery",
  "title": "Cargo Types Collected",
  "chartType": "bar-chart",
  "query": "SELECT COUNT(*) FROM CollectCargo WHERE event_timestamp >= :startDate AND event_timestamp < :endDate",
  "detailQuery": "SELECT COALESCE(Type_Localised, 'Unknown') AS label, COUNT(*) AS value FROM CollectCargo WHERE event_timestamp >= :startDate AND event_timestamp < :endDate GROUP BY Type_Localised ORDER BY value DESC"
}
