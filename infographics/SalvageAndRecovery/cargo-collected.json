{
  "category": "SalvageAndRecovery",
  "title": "Cargo Collected",
  "chartType": "summary-tile",
  "query": "SELECT COUNT(*) AS count, SUM(CASE WHEN Stolen = 1 THEN 1 ELSE 0 END) AS stolen FROM CollectCargo WHERE event_timestamp >= :startDate AND event_timestamp < :endDate",
  "details": {
    "text": "{count} cargo unit(s) scooped · {stolen} stolen",
    "help": "CollectCargo events — mission cargo plus any flagged Stolen=1"
  }
}
